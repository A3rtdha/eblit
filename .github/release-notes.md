## Исправления

Проверка серверов больше не крутит кнопку бесконечно: DNS и curl обрываются по таймауту, пинг всего списка не ждёт зависший зонд.

Удаление из списка снова работает, пока идёт пинг: мост не забивается пачкой `pull()`, запись `config.json` переживает файл, занятый sing-box.
